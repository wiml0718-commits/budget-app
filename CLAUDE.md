# budget-app 專案規則

- 改完程式碼、驗證沒問題後，可以直接 `git add` + commit + push 到 GitHub（origin/main），不用每次都先問，完成後告知結果即可
- 例外：force push、改動 git 歷史、刪除分支等破壞性操作，仍要先問過
- push 完記得等 GitHub Pages 部署完成再回報「好了」，避免使用者太早重新整理看到舊版
